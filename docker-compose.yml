version: '3.8'
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - shared_data:/app/share

  datamanagement:
    build:
      context: ./datamanagement-service
      dockerfile: Dockerfile
    expose:
      - "8080"
    ports:
      - "8080:8080"
    volumes:
      - shared_data:/app/share

volumes:
  shared_data:
